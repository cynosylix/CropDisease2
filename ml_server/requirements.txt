# Dependencies for YOLO-based server (best.pt model)
# Use this for leaf disease detection. Python 3.8+ required.
#
# Required (install with: pip install -r ml_server/requirements.txt)
# -----------------------------------------------------------------------
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
ultralytics>=8.0.0
opencv-python-headless>=4.5.0
numpy>=1.20.0
zeroconf>=0.131.0
